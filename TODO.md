# TODO

Authoritative list of outstanding work items tracked via `// TODO` comments in code. See `CLAUDE.md` → "TODO Tracking".

| File | Method | Note | Asana |
|---|---|---|---|
| `templates/account-membership-bundles/list.php` | (top-level, `$manage_bundle_base_url`) | "Manage Bundle" links currently point at the current URL with a `bundle_post_id` query param as a placeholder. Wire up to the real bundle-detail screen once that template/route exists. | — |
